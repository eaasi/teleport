const {UserInformation} = require('../../../../src/data_access/models/index');

describe('models/userInformation', () => {
	test('It should ...', async () => {
		const userInfo = await UserInformation.findAll()
	})
})
